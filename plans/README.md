# Implementation Plans

Generated by the improve skill on 2026-07-07, against commit `5abb562`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Broken-link check over built site (local + CI) | P1 | S | — | DONE — merged to main |
| 002 | Real README + tools docs + CLAUDE.md | P1 | S | — | DONE — merged to main |
| 003 | Config/tooling hygiene (sitemap filter, overrides guard, partytown removal, sync-resume hardening, .nvmrc) | P1 | S | 001 (soft) | DONE — merged to main |
| 004 | Voyager images via astro:assets | P2 | M | 001 | DONE — merged to main (plan amended: sharp must be a direct devDependency under pnpm) |
| 005 | Blog → content collections + RSS feed | P2 | M | 001 | DONE — merged to main |
| 006 | Mermaid diagrams pre-rendered to committed SVG | P3 | L | 001; 004 | DONE — merged to main |

All six plans were executed by dispatched agents in isolated worktrees,
reviewed and approved, then independently code-reviewed (six parallel
reviews, 2026-07-07). Post-review fixes applied on main after merging:
per-image `sizes`/`widths` correction on the Voyager cover and grid frames;
`[id].astro` renamed to `[...id].astro` for nested-post safety; unique
per-diagram SVG id prefixes (mmdc hardcodes `my-svg`, which collides when
two diagrams inline on one page); `aria-hidden` on inlined diagram SVGs;
regeneration reminders in `.mmd` sources; browser auto-detection in
`tools/render-diagrams.mjs` (replacing a hardcoded chromium path);
`.puppeteerrc.cjs` disabling Puppeteer's browser download repo-wide; docs
reconciled (partytown removal, new scripts, Liberation-fonts requirement,
CI trigger wording). Rejected during fixing: linkinator url-rewrite flags
for OG-card coverage — empirically a no-op (linkinator does not extract
og:image/canonical meta URLs); the gap is documented in CLAUDE.md instead.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 first: it is the URL/asset regression gate that 003–006 cite in their
  verification steps. 003–005 can technically run without it (they note
  which verify lines to skip), but the gate is the point.
- 002 is independent but should land before 005/006, which extend its docs
  (new post location, diagram regeneration workflow).
- 006 after 004: both rewrite parts of the project pages; sequencing avoids
  conflicting diffs. 004 and 005 touch disjoint files and can run in
  parallel worktrees.
- 002, 003, 005, 006 all touch `package.json` scripts/deps — if run in
  parallel, expect trivial merge conflicts there.

## Findings considered and rejected

So nobody re-audits these:

- **Delete the `package.json` `pnpm.overrides` copy** (audit suggestion):
  rejected — it is intentional for Vercel's pnpm 9 (commit `42710b5`);
  deleting it breaks the Vercel build. Plan 003 adds a sync guard instead.
- **MermaidDiagram XSS**: not a finding — `securityLevel: "strict"`, Astro-
  escaped slot content, author-controlled input only.
- **Underscore draft pages (`_photography`, `_music`) leaking**: not a
  finding — Astro excludes them from routing/sitemap; verified absent from
  `dist/`.
- **resume.md / resume-print.md duplication**: not dual-maintenance — both
  generated by `tools/sync-resume.mjs`; the real gap (docs + generated-file
  warning) is covered by plan 002.
- **Remove the Vercel adapter for static output**: not worth doing now —
  MED deploy risk for no measured cost; revisit if the adapter causes
  friction.
- **Commented-out Footer in Layout.astro**: rejected — exists only in git
  history, not current code.
- **Per-entry re-validation of the 8 security pins**: deferred, not planned —
  `pnpm audit` is currently clean; the toggle-and-audit loop is recorded as
  a maintenance note in plan 003.
- **ImageMagick `caption:` input in generate-og-card.mjs**: by-design —
  input is the author's own resume frontmatter.
- **Security category overall**: audited very thoroughly, clean (no
  secrets, no injection, `pnpm audit` zero vulnerabilities as of
  2026-07-07).

## Direction findings not selected as plans

- **Finish `_photography.astro` / `_music.astro`**: gated on real content
  (photos, mix links) only the site owner can supply; the code is one
  un-prefixing away. No plan written — revisit when content exists.
