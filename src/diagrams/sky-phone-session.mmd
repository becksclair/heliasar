%% Generated diagram source - run `pnpm generate:diagrams` after editing and commit the updated SVG.
flowchart TD
	idle["Idle<br>probe the ALSA streams every 2 seconds"] -->|"promoted only when the first PCM chunk<br>is actually bridged, else closed after 1.5 s"| active["Host audio active<br>the phone really opened the headset stream"]
	active -->|"adapter sends enable_bridge<br>forwarding is off by default"| session["Live session<br>20 ms frames bridged both directions<br>overruns and underruns counted, not hidden"]
	session --> ended["Session ended<br>streams torn down, gateway returns to idle"]
